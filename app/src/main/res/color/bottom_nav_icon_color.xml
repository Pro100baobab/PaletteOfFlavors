<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:color="@color/dark_red" android:state_checked="true" />  <!-- Выбранное состояние (активный элемент) -->
    <item android:color="@color/black" android:state_checked="false"/>   <!-- Не выбранное состояние (неактивный элемент) -->
    <item android:color="@color/black"/> <!--  Цвет по умолчанию (если ни одно из состояний не подходит) -->
</selector>