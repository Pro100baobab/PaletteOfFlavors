<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.ПалитраВкусов" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="New.Theme.ПалитраВкусов" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <item name="colorPrimary">@color/bright_red</item>
        <item name="colorPrimaryDark">@android:color/holo_orange_light</item>
        <item name="android:color">@color/white</item>
        <item name="colorAccent">@color/white</item>

    </style>

    <style name="Theme.ПалитраВкусов.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.ПалитраВкусов.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />
    <style name="Theme.ПалитраВкусов.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="Theme.ПалитраВкусов" parent="Base.Theme.ПалитраВкусов" />
</resources>